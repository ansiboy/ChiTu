chitu.register(function (page) {
    /// <param name="page" type="chitu.Page"/>
    //page.showing.add(function () {
    //    var d = $.Deferred();
    //    d.resolve();
    //    return d;
    //});

    //var model = {

    //};

    //return model;
    $(page.container()).append('model1 loaded');
});